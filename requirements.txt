# 视频字幕翻译工具依赖
# 推荐使用: pip install -e .
# 或者使用此文件: pip install -r requirements.txt

-e .

# FFmpeg 需要单独安装 (非 Python 包):
# macOS: brew install ffmpeg
# Ubuntu: sudo apt install ffmpeg
# Windows: 下载 https://ffmpeg.org/download.html
